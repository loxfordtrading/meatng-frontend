# Optional in development (defaults to /api and uses Vite proxy).
# Set this in staging/production deployments as needed.
VITE_API_BASE_URL=https://meatng-api.onrender.com

# Optional: override Vite dev proxy target (used only when API base is /api in development).
VITE_API_PROXY_TARGET=https://meatng-api.onrender.com
